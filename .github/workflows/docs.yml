---
# Canonical docs workflow for zensical docs repos.
# Thin caller of LukeEvansTech/shared-workflows zensical reusable.
# Build-only repos override `publish: false`.
name: Docs

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - ".github/workflows/docs.yml"
  pull_request:
    paths:
      - "docs/**"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  docs:
    uses: LukeEvansTech/shared-workflows/.github/workflows/zensical.yml@dadd5af322f177cba99a056f34c5454d62908d75 # v1
    with:
      publish: ${{ github.event_name != 'pull_request' }}
