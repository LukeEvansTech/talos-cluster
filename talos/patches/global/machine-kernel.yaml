---
# KernelModuleConfig is the 1.14 form, but talhelper 3.1.17 (machinery
# 1.14.0-alpha.2) does not know it yet, so this stays v1alpha1 (deprecated,
# still supported).
machine:
  kernel:
    modules:
      - name: nvidia
      - name: nvidia_uvm
      - name: nvidia_drm
      - name: nvidia_modeset
