---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system
resources:
  - ./ocirepository.yaml
  - ./helmrelease.yaml
  # NOTE: ./runtimeclass.yaml moved to
  # kubernetes/apps/gpu-operator/gpu-operator/app/ during the gpu-operator
  # cutover (PR #2328). Do NOT re-add it here on rollback — the operator
  # path owns RuntimeClass `nvidia` (cluster-scoped, single owner).
