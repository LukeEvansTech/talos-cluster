---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app portabase-agent
  namespace: database
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  values:
    controllers:
      portabase-agent:
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: portabase/agent
              tag: latest@sha256:c73b0e923af2199f027a28ea29fb9592e6ccbaff3d7303abd1b2807a2d8e6bf8
            env:
              TZ: ${TIMEZONE}
            envFrom:
              - secretRef:
                  name: portabase-agent-secret
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: false
            resources:
              requests:
                cpu: 10m
                memory: 64Mi
              limits:
                memory: 256Mi

    defaultPodOptions:
      securityContext:
        runAsNonRoot: false
        runAsUser: 0
        seccompProfile:
          type: RuntimeDefault

    persistence:
      config:
        type: secret
        name: portabase-agent-secret
        globalMounts:
          - path: /config/config.json
            subPath: databases.json
            readOnly: true
