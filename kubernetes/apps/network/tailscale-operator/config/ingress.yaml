---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/tailscale.com/connector_v1alpha1.json
apiVersion: tailscale.com/v1alpha1
kind: Connector
metadata:
  name: ingress
  namespace: network
spec:
  # Configure subnet routes for your network
  # Update these to match your local network subnets
  subnetRouter:
    routes:
      - 192.168.10.0/24
      - 192.168.20.0/24
  replicas: 2
