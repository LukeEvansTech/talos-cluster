---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: &app profilarr
  namespace: media
spec:
  endpoints:
    - dnsName: profilarr.${SECRET_DOMAIN}
      recordTTL: 300
      recordType: A
      targets:
        - ${ENVOY_INTERNAL_IP}
