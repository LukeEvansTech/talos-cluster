---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1.json
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: epicgames
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: epicgames-secret
    template:
      engineVersion: v2
      data:
        EMAIL: "{{ .EPICGAMES_EMAIL }}"
        EMAIL_RECIPIENT_ADDRESS: "{{ .EPICGAMES_EMAIL }}"
  dataFrom:
    - extract:
        key: epicgames
