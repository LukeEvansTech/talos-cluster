---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: minecraft
spec:
  endpoints:
    - dnsName: "minecraft.${SECRET_DOMAIN}"
      recordTTL: 3600
      recordType: A
      targets:
        - "${SVC_MC_ROUTER_ADDR}"
