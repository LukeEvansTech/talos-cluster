---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cronmaster
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: cronmaster
  values:
    controllers:
      cronmaster:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: ghcr.io/fccview/cronmaster
              tag: 2.2.0@sha256:85e843762f2568dee575b85d4fe8d5ec60bad20a9c7f73472701dbf9b9345aa1
            env:
              TZ: ${TIMEZONE}
              NODE_ENV: production
              DOCKER: "true"
              HOST_CRONTAB_USER: root
            envFrom:
              - secretRef:
                  name: cronmaster-secret
            securityContext:
              allowPrivilegeEscalation: true
              readOnlyRootFilesystem: false
              runAsUser: 0
              runAsGroup: 0
              capabilities:
                add:
                  - SYS_ADMIN
            resources:
              requests:
                cpu: 10m
                memory: 256Mi
              limits:
                memory: 512Mi

    defaultPodOptions:
      securityContext:
        runAsNonRoot: false
        runAsUser: 0
        runAsGroup: 0
        fsGroup: 0
        seccompProfile:
          type: RuntimeDefault

    service:
      app:
        controller: cronmaster
        ports:
          http:
            port: 3000

    route:
      app:
        hostnames:
          - "cronmaster.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network

    persistence:
      data:
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /app/data
            subPath: data
          - path: /app/scripts
            subPath: scripts
          - path: /app/snippets
            subPath: snippets
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
