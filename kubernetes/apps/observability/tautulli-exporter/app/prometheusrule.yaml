---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/monitoring.coreos.com/prometheusrule_v1.json
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: tautulli-exporter.rules
spec:
  groups:
    - name: tautulli-exporter.rules
      rules:
        - alert: TautulliExporterDown
          annotations:
            summary: "Tautulli exporter scrape is failing."
          expr: |-
            up{job="tautulli-exporter"} == 0
          for: 10m
          labels:
            severity: warning
        - alert: TautulliUnreachable
          annotations:
            summary: "Tautulli exporter is up but cannot reach the Tautulli API."
          # v1.0.0+ (Go rewrite): on a failed scrape only plex_up and
          # plex_scrape_failures_total move; the stream/bandwidth gauges freeze at
          # their last value instead of disappearing, so absent() never fires.
          expr: |-
            plex_up{job="tautulli-exporter"} == 0
          for: 15m
          labels:
            severity: warning
