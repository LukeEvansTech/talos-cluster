---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/monitoring.coreos.com/servicemonitor_v1.json
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: &app nut-exporter
  labels:
    app.kubernetes.io/name: *app
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: *app
  endpoints:
    - port: metrics
      interval: 30s
      path: /ups_metrics
      params:
        server:
          - network-ups-tools.observability.svc.cluster.local
        ups:
          - apc
      metricRelabelings:
        - action: replace
          sourceLabels:
            - __address__
          targetLabel: instance
          replacement: apc-main
        - action: replace
          replacement: cr-ups-main
          targetLabel: server
        - action: replace
          replacement: apc
          targetLabel: ups
        - action: labeldrop
          regex: pod
