---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helmrelease.yaml
  - ./helmrepository.yaml
  # NOTE: ./grafanadashboard.yaml and ./prometheusrule.yaml moved to
  # kubernetes/apps/gpu-operator/gpu-operator/app/ during the gpu-operator
  # cutover (PR #2328). Do NOT re-add them here on rollback — the operator
  # path owns those resources. Metric names are stable across exporters.
