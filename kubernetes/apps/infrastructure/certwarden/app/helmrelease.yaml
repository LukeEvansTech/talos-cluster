---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: certwarden
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: certwarden
  values:
    controllers:
      certwarden:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          # Install kubectl for post-processing scripts
          install-tools:
            image:
              repository: docker.io/library/alpine
              tag: 3.23.4@sha256:5b10f432ef3da1b8d4c7eb6c487f2f5a8f096bc91145e68878dd4a5019afde11
            command:
              - sh
              - -c
              - |
                set -e

                # Install curl for kubectl download
                echo "Installing curl..."
                apk add --no-cache curl

                # Install kubectl
                echo "Installing kubectl..."
                curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
                chmod +x kubectl
                mv kubectl /shared-bin/

                echo "Installation complete!"
                ls -la /shared-bin/
        serviceAccount:
          identifier: certwarden
        containers:
          app:
            image:
              repository: ghcr.io/gregtwallace/certwarden
              tag: v0.29.2@sha256:ca3550695266e72b37a0f39cc566ea0242b98e5525f6e73ce30db2589442dac8
            env:
              # Add kubectl to PATH for post-processing scripts
              - name: PATH
                value: "/shared-bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
            resources:
              requests:
                cpu: 500m
                memory: 2Gi
              limits:
                memory: 2Gi
    service:
      app:
        controller: certwarden
        ports:
          http:
            port: 4050
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.${SECRET_DOMAIN}"
          - "{{ .Release.Name }}.${SECRET_INTERNAL_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
    serviceAccount:
      certwarden: {}
    persistence:
      data:
        enabled: true
        existingClaim: "{{ .Release.Name }}"
        globalMounts:
          - path: /app/data
      # Shared binaries (kubectl)
      shared-bin:
        type: emptyDir
        globalMounts:
          - path: /shared-bin
      # Post-processing scripts for Supermicro IPMI deployment
      supermicro-scripts:
        type: configMap
        name: certwarden-supermicro-scripts
        defaultMode: 0755
        globalMounts:
          - path: /app/scripts/supermicro
      # Post-processing scripts for APC deployment
      apc-scripts:
        type: configMap
        name: certwarden-apc-scripts
        defaultMode: 0755
        globalMounts:
          - path: /app/scripts/apc
      # Post-processing scripts for Brother printer deployment
      brother-scripts:
        type: configMap
        name: certwarden-brother-scripts
        defaultMode: 0755
        globalMounts:
          - path: /app/scripts/brother
      # Post-processing scripts for NVIDIA Onyx switch deployment
      onyx-scripts:
        type: configMap
        name: certwarden-onyx-scripts
        defaultMode: 0755
        globalMounts:
          - path: /app/scripts/onyx
