---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: infrastructure
resources:
  - externalsecret.yaml
  - rbac.yaml
  - scripts-configmap.yaml

# NOTE: ConfigMap is created as a direct resource (scripts-configmap.yaml) instead of using
# configMapGenerator to avoid Kustomize's variable expansion which replaces ${VAR} with empty strings
