---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-storage-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-storage-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-storage-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-storage-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-01-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-01-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-01-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-01-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-02-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-02-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-02-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-02-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-03-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-03-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-03-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-03-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-04-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-04-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-04-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-04-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-05-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-05-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-05-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-05-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-06-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-06-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-06-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-06-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-07-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-07-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-07-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-07-ipmi
---
# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/external-secrets.io/externalsecret_v1.json
# ExternalSecret for cr-node-08-ipmi Supermicro IPMI credentials
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: supermicro-cr-node-08-ipmi
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: supermicro-cr-node-08-ipmi
    template:
      data:
        IPMI_URL: "{{ .IPMI_URL }}"
        IPMI_MODEL: "{{ .IPMI_MODEL }}"
        IPMI_USERNAME: "{{ .IPMI_USERNAME }}"
        IPMI_PASSWORD: "{{ .IPMI_PASSWORD }}"
        IPMI_IP: "{{ .IPMI_IP }}"
  dataFrom:
    - extract:
        key: cr-node-08-ipmi
