---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: smtp2graph
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: smtp2graph
  values:
    controllers:
      smtp2graph:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: smtp2graph/smtp2graph
              tag: v1.1.5@sha256:88ef2015f37ad460d7cc06fa80cf82a0318108ae696dac61a2896d5016d9545d
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 256Mi
          # smtp2graph v1.1.5 exposes NO metrics of its own — it listens on :25
          # and nothing else, and its config schema has no metrics or health
          # options. This sidecar is the only source of delivery telemetry.
          #
          # It exists because between 2026-08-10 and 2026-08-19 every Graph send
          # failed with ErrorSendAsDenied — 353 messages lost, zero delivered —
          # and nothing noticed for nine days, because the SMTP listener stayed
          # healthy throughout. Liveness was never the problem; delivery was.
          #
          # It reads the queue directories (see persistence.data below) and runs
          # a 6-hourly end-to-end canary through the relay's own :25. Script is
          # a real file under exporter/, not inline YAML, so it can be unit
          # tested — its three scenarios (delivered / rejected by Graph / relay
          # down) were each exercised against a stub SMTP server before merge.
          metrics:
            image:
              repository: python
              tag: 3.14-alpine@sha256:05b2b8b732ecd268fee8727a369f936f022d1321b59befd13c30ede22769dcdc
            command:
              - "python"
              - "-u"
              - "/opt/exporter/exporter.py"
            # Container port, NOT a second Service. app-template 5.x renames EVERY
            # service the moment there is more than one (smtp2graph ->
            # smtp2graph-app), which NXDOMAINs
            # smtp2graph.infrastructure.svc.cluster.local — the address
            # pocket-id, tandoor, epicgames and scanopy all use, and it takes
            # the LoadBalancer VIP down with it. `primary: true` does not opt
            # out. So metrics are scraped straight off the pod by podmonitor.yaml
            # and never touch the LAN-facing Service.
            ports:
              - name: metrics
                containerPort: 9184
                protocol: TCP
            env:
              MAILROOT: /data/mailroot
              METRICS_PORT: "9184"
              SMTP_HOST: 127.0.0.1
              SMTP_PORT: "25"
              CANARY_ENABLED: "true"
              # 6h. The canary sends REAL mail to CANARY_TO — a mailbox rule on
              # the "[canary] smtp2graph" subject keeps it out of the way.
              # Shorten for a tighter detection window at the cost of more mail.
              CANARY_INTERVAL_SECONDS: "21600"
              CANARY_TIMEOUT_SECONDS: "180"
              # MUST be listed in allowedFrom (see externalsecret.yaml) or the
              # canary is rejected at MAIL FROM and reports a false outage.
              CANARY_FROM: noreply@${SECRET_DOMAIN}
              CANARY_TO: operations@${SECRET_DOMAIN}
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  httpGet: { path: /metrics, port: 9184 }
                  initialDelaySeconds: 20
                  periodSeconds: 60
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  httpGet: { path: /metrics, port: 9184 }
                  initialDelaySeconds: 10
                  periodSeconds: 30
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities: { drop: ["ALL"] }
            resources:
              requests:
                cpu: 5m
              limits:
                memory: 64Mi
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
    service:
      app:
        type: LoadBalancer
        annotations:
          external-dns.alpha.kubernetes.io/hostname: smtp.${SECRET_DOMAIN}
          io.cilium/lb-ipam-ips: ${SVC_SMTP_RELAY_ADDR}
        # No externalTrafficPolicy: Local here (unlike maddy): with one replica the
        # Cilium L2 lease regularly lands on a node without the pod, which drops
        # every connection. Client source IP is irrelevant on the Graph path.
        ports:
          smtp:
            port: 25
    persistence:
      # WORKDIR is /data: config.yml is read from here and the disk queue
      # (queue/ temp/ failed/) is created alongside it. emptyDir matches the
      # maddy smtp-relay's queue durability — acceptable at estate volume.
      # advancedMounts, not globalMounts: the sidecar must NOT see client.key.
      # globalMounts would hand the Graph signing key to a container whose only
      # job is counting files, so each mount is now targeted at the container
      # that actually needs it.
      data:
        type: emptyDir
        advancedMounts:
          smtp2graph:
            app:
              - path: /data
            metrics:
              - path: /data
                readOnly: true
      config:
        type: secret
        name: smtp2graph-secret
        advancedMounts:
          smtp2graph:
            app:
              - path: /data/config.yml
                subPath: config.yml
                readOnly: true
              - path: /secrets/client.key
                subPath: client.key
                readOnly: true
      exporter:
        type: configMap
        name: smtp2graph-exporter
        advancedMounts:
          smtp2graph:
            metrics:
              - path: /opt/exporter/exporter.py
                subPath: exporter.py
                readOnly: true
      tmp:
        type: emptyDir
        globalMounts:
          - path: /tmp
