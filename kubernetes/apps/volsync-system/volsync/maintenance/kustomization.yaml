---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./externalsecret-nfs.yaml
  - ./externalsecret-r2.yaml
  # Temporarily disabled until volsync upgrades to perfectra1n image with kopia CRDs
  # - ./kopiamaintenance-nfs.yaml
  # - ./kopiamaintenance-r2.yaml
